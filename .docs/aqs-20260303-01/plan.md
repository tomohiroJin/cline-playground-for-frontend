# Agile Quiz Sugoroku ブラッシュアップ計画 (Phase 2)

## 日付: 2026-03-03（更新: 2026-03-04）

## Context

前回ブラッシュアップ (aqs-20260223-01) でタカキャラクター追加・スプリント数選択・クイズ問題追加等を実施済み。
今回は以下を実施する：

1. ~~UI の画像表示スタイル修正~~ → 画像自体のつぶれが原因と判明、画像差し替えで対応
2. TEAMメンバー順番修正（完了）
3. **新キャラクター「ペンギン」（スクラムマスター）追加**
4. **全キャラクター画像の統一リニューアル**
5. すごろく要素の強化案ドキュメント

## 背景: PO/SM 分離

現在イヌが「PO / スクラムマスター」を兼任しているが、スクラムガイドでは PO と SM の兼任はアンチパターンとされている。
イヌを PO 専任にし、新キャラクター「ペンギン」をスクラムマスター専任として追加する。

## 実装順序

### Phase 2a（完了済み）
1. ~~ドキュメント作成（plan.md, spec.md, tasks.md）~~ ✅
2. ~~TEAMメンバー順番修正~~ ✅
3. ~~画像スタイル微調整~~ ✅（ただし根本原因は画像自体）

### Phase 2b（新規）
1. ペンギン（スクラムマスター）のキャラクター定義追加
   - `character-profiles.ts` にプロフィール追加
   - `images.ts` に画像 import 追加
2. イヌの role を「PO / スクラムマスター」→「プロダクトオーナー」に変更
3. TEAMメンバー順番を最終確定: タカ→イヌ→ペンギン→ネコ→ウサギ
4. 全キャラクター画像の統一リニューアル（ユーザー作業：画像生成 → 配置）
5. テスト実行で動作確認

## 変更対象ファイル

### コード変更
- `character-profiles.ts` - ペンギン追加、イヌの role 変更、配列順変更
- `images.ts` - ペンギン画像の import 追加
- `constants.ts` - `getSummaryText` のペンギン関連テキスト対応（必要に応じて）

### 画像ファイル（ユーザー作業：生成 → 配置）
- `aqs_char_penguin.webp` - ペンギン個別画像（新規）
- `aqs_char_taka.webp` - タカ個別画像（差し替え）
- `aqs_char_inu.webp` - イヌ個別画像（差し替え）
- `aqs_char_neko.webp` - ネコ個別画像（差し替え）
- `aqs_char_usagi.webp` - ウサギ個別画像（差し替え）
- `aqs_char_team.webp` - チームバナー（差し替え：5キャラ統一デザイン）
- `aqs_char_group.webp` - グループ画像（差し替え：5キャラ統一デザイン）
- `aqs_title.webp` - タイトル背景（差し替え：5キャラ統一デザイン）

## 依存関係

```
画像プロンプト作成 ──→ 画像生成（ユーザー作業）──→ 画像配置
         ↓
ペンギンキャラ定義追加 ──→ イヌ role 変更 ──→ 順番確定 ──→ テスト
```

- コード変更は画像の有無に関係なく先行実施可能（emoji フォールバックあり）
- 画像生成・配置はユーザー作業

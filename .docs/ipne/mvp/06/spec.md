# MVP06 仕様書: 鍵システム・ボスHP調整・職業画像表示

## 概要

MVP06では以下の3つの機能を実装した:
1. ボスHPの調整（レベル上げ前に倒されにくくする）
2. 鍵システムの実装（ゴール条件の追加と職業差別化）
3. 職業選択画面の画像表示

---

## 1. ボスHP調整

### 変更内容
- **ボスHP: 12 → 35**

### 根拠
| 状況 | 攻撃力 | 必要攻撃回数 | リスク |
|------|--------|--------------|--------|
| 戦士Lv1 | 2 | 18回 | 高（死亡リスク大） |
| 戦士Lv2 | 3 | 12回 | 中（回復で生存可能） |
| 盗賊Lv1 | 1 | 35回 | 確実に死亡 |

### 修正ファイル
- `src/features/ipne/enemy.ts`: `ENEMY_CONFIGS[EnemyType.BOSS].hp` を35に変更

---

## 2. 鍵システム

### 概要
ゴールするには「鍵」が必要。鍵の入手方法は2つ:

1. **マップ上の鍵を拾う** - ゴールから離れた位置に1つ配置（盗賊向け）
2. **ボスを倒して鍵を入手** - ボス撃破時に必ずドロップ（戦士向け）

### 職業差別化
- **戦士**: 高攻撃力でボスを倒して確実に鍵GET
- **盗賊**: 高移動速度・罠視認能力で探索して鍵GET

### 実装詳細

#### 2.1 新アイテムタイプ
```typescript
export const ItemType = {
  // 既存...
  KEY: 'key',
} as const;
```

#### 2.2 プレイヤー状態
```typescript
export interface Player {
  // 既存フィールド...
  hasKey: boolean;  // 鍵所持フラグ
}
```

#### 2.3 アイテム処理
- `createKeyItem(x, y)`: 鍵アイテム作成
- `pickupItem`: KEY タイプ時に `player.hasKey = true` を設定
- `spawnItems`: ゴールから15マス以上離れた位置に鍵を配置

#### 2.4 ボスドロップ
- `processEnemyDeath`: ボス撃破時は必ず鍵をドロップ

#### 2.5 ゴール判定
```typescript
export const canGoal = (player: Player): boolean => {
  return player.hasKey;
};
```
- ゴール地点に立っても鍵がなければクリアしない
- 「鍵が必要です」メッセージを2秒間表示

### UI
- **鍵インジケータ**: 右上に表示、取得前は半透明・グレースケール、取得後は明るく金色
- **鍵アイテム**: 金色（#fcd34d）で描画

---

## 3. 職業選択画面の画像表示

### 変更内容
- 絵文字（⚔️, 🗡️）の代わりに画像を表示
- `ipne_class_warrior.webp`: 戦士画像
- `ipne_class_thief.webp`: 盗賊画像

### 画像仕様
- フォーマット: WebP（品質90%）
- サイズ: 1024x1024（元画像）、CSS で 80x80px に表示
- 配置: `src/assets/images/`

---

## 修正ファイル一覧

### 型定義
- `src/features/ipne/types.ts`
  - `ItemType.KEY` 追加
  - `Player.hasKey` 追加

### ゲームロジック
- `src/features/ipne/enemy.ts`
  - ボスHP: 12 → 35
  - `processEnemyDeath`: ボス鍵ドロップ追加
- `src/features/ipne/player.ts`
  - `createPlayer`: `hasKey: false` 追加
- `src/features/ipne/item.ts`
  - `ITEM_CONFIGS[KEY]` 追加
  - `createKeyItem()` 追加
  - `pickupItem`: KEY 処理追加
  - `spawnItems`: 鍵配置ロジック追加
- `src/features/ipne/goal.ts`
  - `canGoal()` 追加
- `src/features/ipne/index.ts`
  - エクスポート追加

### UI
- `src/pages/IpnePage.tsx`
  - インポート追加
  - ゲーム初期化時に鍵配置
  - ゴール判定に `canGoal` チェック追加
  - 「鍵が必要です」メッセージ表示
  - 鍵インジケータ追加
  - ボス撃破時のドロップ処理追加
  - 職業選択画面の画像表示
- `src/pages/IpnePage.styles.ts`
  - `KeyIndicator` 追加
  - `KeyIcon` 追加
  - `KeyRequiredMessage` 追加
  - `ClassImage` 追加

### テスト
- `src/features/ipne/__tests__/enemy.test.ts`
  - ボスHP期待値を35に変更
  - ボス鍵ドロップテスト追加

### 画像
- `src/assets/images/ipne_class_warrior.webp`（新規）
- `src/assets/images/ipne_class_thief.webp`（新規）

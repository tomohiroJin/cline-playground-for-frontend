// Race ドメイン定数

export const RACE_TIMING = Object.freeze({
  COUNTDOWN: 3500,
  LAP_ANNOUNCE: 1500,
  DEMO: 15000,
  IDLE: 8,
});

/** ゲーム全体のドメイン定数 */
export const GAME = Object.freeze({
  TRACK_WIDTH: 55,
  COLLISION_DIST: 25,
  CHECKPOINT_RADIUS: 90,
  WALL_WARP_THRESHOLD: 10,
});

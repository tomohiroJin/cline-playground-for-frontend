// 落ち物シューティング カスタムフック（後方互換性のための再エクスポート）
// 各フックは hooks/ ディレクトリの個別ファイルに移動済み

export { useInterval } from './hooks/use-interval';
export { useKeyboard } from './hooks/use-keyboard';
export { useIdleTimer } from './hooks/use-idle-timer';

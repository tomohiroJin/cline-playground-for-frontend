/**
 * 灰燼の城壁 - 配色トークン（設計書 §9.4）
 *
 * 60-30-10。赤は危険専用に予約し、枠やパネルには使わない。
 * 前作は赤を主色に使ったため、リアルタイムで本当の危険を赤で伝えられなくなる
 * 問題を抱えていた。
 */
export const COLORS = {
  /** ドミナント60%: 灰燼の暗色。背景・盤面の地 */
  dominant: '#1a1614',
  /** セカンダリ30%: 灰白。テキスト・枠・パネル */
  secondary: '#e8ded2',
  /** アクセント10%: 危険専用（ライフ低下・被弾・警告） */
  danger: '#8b2635',
  /**
   * 危険専用・テキスト用（明るい赤系）。dominant背景（#1a1614）に対し
   * コントラスト比 6.15:1 で WCAG AA（4.5:1）を満たす。danger はそのままだと
   * 背景との比が 2.08:1 しかなく文字色には使えないため分離した（指摘再発防止）。
   */
  dangerText: '#e8737f',
  /** アクセント: 好機（配置可能・再点火可能・要操作） */
  opportunity: '#e8a33d',
  /** 盤面のセル境界 */
  grid: '#3a322c',
} as const;

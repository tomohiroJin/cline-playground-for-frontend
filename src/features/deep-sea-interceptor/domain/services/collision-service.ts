// ============================================================================
// Deep Sea Interceptor - 衝突判定サービス（純粋関数）
// ============================================================================

// 既存の collision.ts を re-export（後方互換性を維持しつつドメイン層で参照可能に）
export { Collision, distance } from '../../collision';
